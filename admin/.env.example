# Admin dev server. In docker-compose dev, the API is reachable as the `api` service.
# Locally (npm run dev) the default points at the standalone api port (3100).
VITE_API_PROXY=http://localhost:3100
